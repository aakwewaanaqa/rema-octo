#!/usr/bin/env ruby
# frozen_string_literal: true

require "json"
require_relative "../src/src"
require_relative "../src/lsp/document_store"
require_relative "../src/lsp/handler"
require_relative "../src/lsp/server"

store   = Lsp::DocumentStore.new
handler = Lsp::Handler.new(store)
server  = Lsp::Server.new(handler)
server.run
